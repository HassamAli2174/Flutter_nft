import 'package:flutter/material.dart';

Color whiteColor = Colors.white;
Color blackColor = Colors.black;
// Color primaryColor = Color(0xff94ed34);
Color primaryColor = Colors.cyan;
Color appGradient = const LinearGradient(
  colors: [
    Color(0xFF8EC5FC),
    Color(0xFFE0C3FC),
  ],
) as Color;
